# CI subset. Deliberately excludes torch, sentence-transformers, qdrant-client
# and langgraph: those add ~1GB and several minutes for tests that skip
# themselves when the dependency is absent (pytest.importorskip). Fast CI gets
# run; slow CI gets disabled.
requests>=2.32
python-dotenv>=1.0
duckdb>=1.1
pyyaml>=6.0
pandas>=2.2
numpy>=1.24
pyarrow>=15.0
lxml>=5.0
pydantic>=2.9
scikit-learn>=1.5
scipy>=1.11
rank-bm25>=0.2.2
pytest>=8.3
